export class PLocation {
    constructor(locationName: string,
        map: string) {
        this.locationName = locationName;
        this.map = map;
    }
    IDLocation: number;
    locationName: string;
    map: string;
}